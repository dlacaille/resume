% Change encoding to utf8 to support accents
\usepackage[utf8]{inputenc}
% Use the classicthesis style for the style of the document
\usepackage[nochapters,pdfspacing]{classicthesis}
% Use the currvita style for the layout of the document
\usepackage[LabelsAligned]{currvita}

%\usepackage[french]{babel} % Change language here
\usepackage{geometry}
\usepackage{ragged2e}
\usepackage{enumitem}
\usepackage{wrapfig}
\usepackage{titlesec}
\usepackage[upright]{fourier}
\usepackage{lib/tkz-kiviat}

\usetikzlibrary{arrows}

% Set up item lists
\setlist[itemize]{itemsep=0.5em, parsep=0em}
\AtBeginDocument{\def\labelitemi{$\bullet$}}

% Configure fonts
\usepackage{lmodern}
%\usepackage[default,scale=0.95]{opensans}
\renewcommand{\baselinestretch}{1.5}

% Set up fontawesome for icons
\usepackage{fontawesome5}

% Move the margin to the left of the page
\reversemarginpar
% New command defining the margin text style
\newcommand{\MarginText}[1]{%
    \marginpar{\raggedleft\itshape\small#1}%
}

% Configure automatic date counters
\usepackage{datenumber}

\newcounter{dateone}
\newcounter{datetwo}

\newcommand{\yearssince}[3]{%
    \setmydatenumber{dateone}{\the\year}{\the\month}{\the\day}%
    \setmydatenumber{datetwo}{#1}{#2}{#3}%
    \addtocounter{datetwo}{-\thedateone}%
    \the\numexpr-\thedatetwo/365\relax\space%
}

% Set up page dimensions
\geometry{
    top=30mm,
    bottom=30mm,
    left=45mm,
    right=30mm,
    heightrounded
}

% Set up header and footer
\ohead[]{\headmark}
\cfoot[\pagemark]{\pagemark}

% Font color of your name at the top
\renewcommand{\cvheadingfont}{\LARGE\color{RoyalBlue}}

% Font and spacing for section headers 
\titlespacing*{\section}
{0pt}{1em}{1em}

\usepackage{hyperref} % Required for adding links	and customizing them
\hypersetup{colorlinks, breaklinks, urlcolor=RoyalBlue, linkcolor=RoyalBlue} % Set link colors

\newlength{\datebox}\settowidth{\datebox}{Jan 2022--Jan 2023 } % Set the width of the date box in each block

% Define a command for each new block - change spacing and font sizes here: #1 is the left margin, #2 is the italic date field and #3 is the position/employer/location field
\newcommand{\NewEntry}[3]{%
    \noindent\hangindent=2em\hangafter=0 \parbox{\datebox}{\small \textit{#1}} \hspace{1.5em} #2 #3 \vspace{0.5em}%
}

% Define a command for descriptions of each entry - change spacing and font sizes here
\newcommand{\Description}[1]{%
    \hangindent=2em\hangafter=0\justifying\noindent\footnotesize{#1}\par\normalsize\vspace{1em}%
}

% Define a command for descriptions of each entry - change spacing and font sizes here
\newcommand{\IconText}[2]{%
    \hspace{-0.2em}\footnotesize\textcolor{gray}{#1}\hspace{0.5em} #2%
}

% Define a command for typesetting C++
\newcommand{\Cpp}{C\nolinebreak\hspace{-.05em}\raisebox{.4ex}{\tiny +}\nolinebreak\hspace{-.1em}\raisebox{.4ex}{\tiny +}\hspace{.2em}}

% Define a command for work experience years
\newcommand{\WorkExp}[0]{%
    \yearssince{2010}{09}{01}%
}
